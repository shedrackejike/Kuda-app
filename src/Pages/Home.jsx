import { Flex } from '@chakra-ui/react'
import React from 'react'

const Home = () => {
  return (
   <Flex bg={'gray'} h={'100vh'}>
    <Flex>
        <Flex>

        </Flex>
    </Flex>
   </Flex>
  )
}

export default Home
